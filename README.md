# csci-6234

Start API:

```
cd Project/api/
conda activate csci-6234
uvicorn main:app --reload
```

Start CLI:

```
cd Project/cli/
install bun - curl -fsSL https://bun.com/install | bash
bun run index.ts
```
